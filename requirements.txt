# ========================================
# 天衍 - 依赖清单
# ========================================
# 核心依赖 (必装, 全平台纯 Python, 无需 C 编译器)
# ========================================
fastapi>=0.110
uvicorn>=0.27
litellm>=1.40
openai>=1.0
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0
python-multipart>=0.0.9
httpx>=0.27
python-dotenv>=1.0

# ========================================
# 文件格式支持 (纯 Python, Windows 友好)
# ========================================
python-docx>=1.1
pypdf>=4.0
ebooklib>=0.18
beautifulsoup4>=4.12
Markdown>=3.5

# ========================================
# 记忆系统 (多项目隔离)
# PostgreSQL: 项目/角色/章节持久化 (不可用时降级到 SQLite)
# Redis: 短期对话记忆 (不可用时降级到内存)
# ChromaDB: 向量语义检索 (不可用时降级到关键词检索)
# ========================================
redis>=5.0
psycopg2-binary>=2.9
chromadb>=0.4

# ========================================
# 沙箱安全 (代码执行隔离)
# RestrictedPython: Python 级代码预检
# 注: pypy3 是系统级安装 (Dockerfile 中通过 apt 安装), 不在 pip
# ========================================
RestrictedPython>=7.0
# 注: pypy3 是系统级安装 (Dockerfile 中通过 apt 安装), 不在 pip 中
# pypy3-restricted 包在 PyPI 上不存在, pypy3 由 Dockerfile apt install 提供

# ========================================
# 高性能组件 (仅 Linux/macOS, Windows 自动跳过)
# ========================================
uvloop>=0.14; sys_platform != 'win32'
httptools>=0.5; sys_platform != 'win32'
