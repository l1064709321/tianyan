# ========================================
# 天衍 - Windows 依赖 (完整版)
# ========================================
# 安装: pip install -r requirements-win.txt
# 
# Windows 兼容说明:
#   - uvloop/httptools: 仅 Linux/macOS, 已跳过
#   - psycopg2-binary: 需要 C 编译器, 用 SQLite 替代
#   - playwright: 需要额外下载浏览器内核, 按需安装
# ========================================

# ---- 核心 (必装) ----
fastapi>=0.110
uvicorn>=0.27
litellm>=1.40
openai>=1.0
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0
python-multipart>=0.0.9
httpx>=0.27
python-dotenv>=1.0

# ---- 文件格式支持 ----
python-docx>=1.1
pypdf>=4.0
ebooklib>=0.18
beautifulsoup4>=4.12
Markdown>=3.5

# ---- 网页抓取 ----
readability-lxml>=0.8
lxml>=5.0

# ---- 向量语义检索 (需要) ----
chromadb>=0.4

# ---- 记忆系统 ----
# Redis: 对话短期记忆, Windows 可选
# redis>=5.0
# PostgreSQL: 项目持久化, 用 SQLite 替代 (store.py 已支持)
# psycopg2-binary>=2.9

# ---- 沙箱安全 ----
RestrictedPython>=7.0

# ---- 浏览器抓取 (按需) ----
# playwright>=1.40
# 安装后需要: playwright install chromium
