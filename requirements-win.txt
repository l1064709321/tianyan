# ========================================
# 天衍 - Windows 完整依赖
# ========================================
# 安装: pip install -r requirements-win.txt
# 
# 包含全部功能:
#   - 大模型调用 (DeepSeek/OpenAI/Gemini 等)
#   - 7 Agent 协同 + 5 阶段写作流水线
#   - 技能内核 (审计/拆解/仿写/代笔)
#   - 向量语义检索 (chromadb)
#   - 项目数据持久化 (SQLite, 自动)
#   - 对话记忆 (Redis/内存)
#   - 网页抓取 (httpx + readability)
#   - 文件读写 (txt/docx/pdf/epub)
#   - 沙箱安全 (RestrictedPython)
# ========================================

# ---- 核心 ----
fastapi>=0.110
uvicorn>=0.27
litellm>=1.40
openai>=1.0
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0
python-multipart>=0.0.9
httpx>=0.27
python-dotenv>=1.0

# ---- 文件格式 ----
python-docx>=1.1
pypdf>=4.0
ebooklib>=0.18
beautifulsoup4>=4.12
Markdown>=3.5

# ---- 网页抓取 ----
readability-lxml>=0.8
lxml>=5.0

# ---- 向量语义检索 ----
chromadb>=0.4

# ---- 记忆系统 ----
redis>=5.0
psycopg2-binary>=2.9

# ---- 沙箱安全 ----
RestrictedPython>=7.0

# ---- 浏览器抓取 (可选, 安装后需执行 playwright install chromium) ----
playwright>=1.40
